<UserControl x:Class="Wpf.BLPmodel.Pages.OtherPages.Views.WriteNoteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Wpf.BLPmodel.Pages.OtherPages.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="45"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid Grid.Column="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="121"/>
                <ColumnDefinition Width="47"/>
            </Grid.ColumnDefinitions>

            <Button Command="{Binding AddNoteCommand}"  Margin="0,0,0,0"  Background="White" Width="45" HorizontalAlignment="Right" Grid.Column="2">
                <Image Source="/Resources/check.png" Height="35" />
            </Button>
            <TextBox Text="{Binding NameNote}" FontSize="25" Grid.Column="0" Margin="0,6,2,6" ></TextBox>
            <ComboBox  ItemsSource="{Binding ComboSource}" SelectedItem="{Binding SelectedSecFlag}"  IsEditable="True" Grid.Column="1" IsReadOnly="True" Text="Уровень доступа" HorizontalAlignment="Right" Margin="0,10,0,10" Width="121"  />

        </Grid>



        <Grid  Grid.Row="1">
            <ScrollViewer  Margin="0,2,0,0" >
                <TextBox Text="{Binding DataNote}" TextWrapping="Wrap" AcceptsReturn="True" Foreground="LightSeaGreen" Margin="0,2,0,0" FontSize="16"  ScrollViewer.VerticalScrollBarVisibility="Hidden" SpellCheck.IsEnabled="True" Language="ru-ru" TextAlignment="Left" >

                </TextBox>
            </ScrollViewer>
        </Grid>

    </Grid>
</UserControl>
